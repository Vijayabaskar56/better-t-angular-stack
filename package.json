{
  "name": "with-angular",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@8.14.0",
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "dependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
